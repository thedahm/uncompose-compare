{
  "name": "uncompose-compare",
  "private": true,
  "scripts": {
    "build:frontend": "npm --prefix frontend ci && npm --prefix frontend run build",
    "build": "npm run build:frontend && cargo build --release",
    "wheel": "bash scripts/build-wheel.sh",
    "clean-install-proof": "bash scripts/clean-install-proof.sh",
    "pipeline": "bash scripts/pipeline.sh",
    "typecheck": "npm --prefix frontend run typecheck",
    "test": "npm run build:frontend && cargo test",
    "serve": "npm run build:frontend && cargo run"
  },
  "dependencies": {
    "@ai-hero/sandcastle": "^0.12.0",
    "zod": "^4.4.3"
  }
}
